# Flip to false once the real backend is available
VITE_USE_MOCKS=true

VITE_API_BASE_URL=/api

# WebSocket
VITE_WS_URL=/api/notifications/stream
VITE_WS_DESTINATION=/user/queue/notifications
VITE_WS_TRANSPORT=raw
