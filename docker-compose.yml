version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: pg-test-5434
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: eventsdb